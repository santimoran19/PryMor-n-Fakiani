// Estructurar los datos que se envían al cliente.
// Por ahora el controlador devuelve directamente lo que trae el modelo con
// res.json(), no hace falta transformar nada. Se deja este archivo preparado
// para el día que haya que dar un formato distinto a la respuesta
// (por ejemplo, ocultar columnas internas o renombrar campos).
